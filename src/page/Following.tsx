import React from 'react';
import { Loading } from '../components/loading';

const Following = () => {
    return (
        <div className="w-[692px] pt-[64px] mx-4 h-screen overflow-auto flex items-center justify-center">
            <Loading />
        </div>
    );
};

export default Following;
